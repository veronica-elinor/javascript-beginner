var flag = true;
while (flag)
{
    var name = prompt("name: ", "default");
    if (confirm("year name: " + name + "?"));
    {
        flag = false;
    }
}
alert("hi" + name + "welcome!");
